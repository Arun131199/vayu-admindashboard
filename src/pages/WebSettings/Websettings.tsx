import { Outlet } from "react-router-dom";

export default function Websettings() {
    return (
        <main>
            <section>
                <Outlet />
            </section>
        </main>
    )
}